# Application properties
app.name=ADBTool
app.version=1.0.0

# Server settings
server.ip=127.0.0.1
server.port=8080

# Database settings
database.url=jdbc:mysql://localhost:3306/mydatabase
database.username=root
# The database password should be set via an environment variable

# Logging settings
logging.level=INFO
logging.filepath=/var/log/adbtool.log
